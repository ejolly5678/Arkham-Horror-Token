'use strict';

describe('myApp.easy module', function() {

  beforeEach(module('myApp.easy'));

  describe('easy controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var easyCtrl = $controller('easyCtrl');
      expect(easyCtrl).toBeDefined();
    }));

  });
});
